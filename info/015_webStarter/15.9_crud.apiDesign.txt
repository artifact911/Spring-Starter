Рекомендованные именования:
    GET
        - если возвращаем лист, то /employees
        - если одну сущность - /employees/{id}

Для других методов не нужно использовать глагольные имена, смысл передает назнание метода:
    POST
        - /employees
    PUT
        - /employees/{id}